{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": { "maxLambdaSize": "2048mb" },
      "builds": [{ "src": "**/*.ts", "use": "ts-node" }]
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/index.ts" }
  ]
}
